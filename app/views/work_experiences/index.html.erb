<p id="notice"><%= notice %></p>

<h1>Work Experiences</h1>

<table>
  <thead>
    <tr>
      <th>Resume</th>
      <th>Name</th>
      <th>Scholl</th>
      <th>Begin date</th>
      <th>End date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @work_experiences.each do |work_experience| %>
      <tr>
        <td><%= work_experience.resume %></td>
        <td><%= work_experience.name %></td>
        <td><%= work_experience.scholl %></td>
        <td><%= work_experience.begin_date %></td>
        <td><%= work_experience.end_date %></td>
        <td><%= link_to 'Show', work_experience %></td>
        <td><%= link_to 'Edit', edit_work_experience_path(work_experience) %></td>
        <td><%= link_to 'Destroy', work_experience, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Work Experience', new_work_experience_path %>
